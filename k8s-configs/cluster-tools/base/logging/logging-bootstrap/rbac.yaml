apiVersion: v1
kind: ServiceAccount
metadata:
  name: logging-bootstrap-serviceaccount

---

apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: logging-bootstrap-role
rules:
  - apiGroups:
    - ""
    resources:
    - "secrets"
    - "configmaps"
    - "storageclasses"
    verbs:
    - create
    - update

---

apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: logging-bootstrap-role-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: logging-bootstrap-role
subjects:
  - kind: ServiceAccount
    name: logging-bootstrap-serviceaccount